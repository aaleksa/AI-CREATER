{
  "name": "auteur",
  "private": true,
  "scripts": {
    "dev": "npm run start --prefix backend & npm run dev --prefix frontend; wait",
    "dev:api": "npm run start --prefix backend",
    "dev:web": "npm run dev --prefix frontend"
  }
}
